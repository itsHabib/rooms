---
driver_version: 1
generated_at: 2026-07-23T00:00:00Z
generated_by: work-driver-prep
source:
  project: rooms
  phase: 03-custody-plane
repo: rooms
repo_url: https://github.com/itsHabib/rooms
branch_prefix: egress-
default_runtime: local

batches:
  - id: 1
    label: zero-egress enforcement
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KY6SM30MGXV2DTHF8A4RE1AG
        task_slug: zero-egress-rooms
        spec_path: docs/features/zero-egress-rooms/spec.md
        branch_name: egress-zero-egress-rooms
        runtime: local
        model: opus
        effort: max
        touches:
          - src/egress.rs
          - src/main.rs
          - src/firecracker.rs
          - src/artifacts.rs
          - src/lib.rs
          - tests/egress_e2e.rs
        status: done
        pr_number: 82
        merge_commit: 3b6e88be3994555b5b5aded7a8c7ac63aba15a46
        merged_at: 2026-07-23T20:29:56Z
        # e2e: 6/6 pass on rooms-host (validated out of band); test-IP + AppArmor hardening merged in #83.
  - id: 2
    label: egress-control validation harness
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KY6T07VEQVKWR20Y4GCPHDY6
        task_slug: egress-control-test-harness
        spec_path: docs/features/egress-control-test-harness/spec.md
        branch_name: egress-control-test-harness
        runtime: local
        model: opus
        effort: max
        touches:
          - src/egress_audit.rs
          - src/lib.rs
          - tests/egress_control_e2e.rs
          - tests/fixtures/egress-control/
        status: pending

conflict_notes:
  - kind: dep_signal
    from: egress-control-test-harness
    to: zero-egress-rooms
    reason: "the harness validates the --egress control zero-egress-rooms builds; batch 2 depends_on batch 1, which is now merged (#82) — so batch 2 is ready now."

skipped_during_resolution: []
---

# Custody plane driver manifest

Generated by `/work-driver-prep` on 2026-07-23 (batch 1: zero-egress; batch 2: harness).
Consumed by `/work-driver docs/features/03-custody-plane/driver.md`.

## Batches

### Batch 1 — zero-egress enforcement — ✅ DONE

- `tsk_01KY6SM30MGXV2DTHF8A4RE1AG` → `docs/features/zero-egress-rooms/spec.md`
  - **merged**: PR #82 (`3b6e88b`), local / opus / max. e2e 6/6 on rooms-host; hardening in #83.
  - `--egress none|allowlist` host-side enforcement keyed on the ingress tap, proof-of-absence receipt.

### Batch 2 — egress-control validation harness — ready now (batch 1 merged)

- `tsk_01KY6T07VEQVKWR20Y4GCPHDY6` → `docs/features/egress-control-test-harness/spec.md`
  - branch `egress-control-test-harness`
  - **runtime: local** — single-repo; the scorer core is unit-test-driven (`make check`, CI gate), the full fixture→room→score loop is host-only e2e on the rooms-host out of band (same posture as batch 1). No cloud signal earns the slot.
  - **model: opus / effort: max** — security-validation tooling. A scorer that misreads the witness silently *certifies a control that may not hold* — a silent, expensive failure mode; plus the fixture-realism judgment and the two-condition-gate design want careful reasoning.
  - **⚠ split-at-review likely** (spec est. ~520 weighted, stretch): land Part A (scorer + sentinel registry + unit tests + 2–3 fixtures + the two-condition gate proven on one fixture) first, then Part B (corpus to ≥6 fixtures + cross-config scorecard).
  - touches: `src/egress_audit.rs` (new), `src/lib.rs`, `tests/egress_control_e2e.rs` (new), `tests/fixtures/egress-control/**` (new)

## Conflict scan

- **File overlap**: none between batches — batch 1 is merged; batch 2's `src/egress_audit.rs` + fixtures are new files. Batch 2 touches `src/lib.rs` (a module decl add), disjoint from batch 1's now-merged edit.
- **Dep signals**: 1 edge — batch 2 `depends_on` batch 1 (validates the `--egress` control). Batch 1 is merged, so **batch 2 is ready now**.

## Reachability

Batch 2 is **local runtime**, so its worktree forks from `origin/main` — the spec `docs/features/egress-control-test-harness/spec.md` (+ this manifest update) must be on `main` before `/work-driver` fans out. Land via a small docs PR (`docs(custody-plane): spec for egress-control test harness`); never push `main` directly.
