---
driver_version: 1
generated_at: 2026-07-23T00:00:00Z
generated_by: work-driver-prep
source:
  project: rooms
  phase: 03-custody-plane
repo: rooms
repo_url: https://github.com/itsHabib/rooms
branch_prefix: egress-
default_runtime: local

batches:
  - id: 1
    label: ready now
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KY6SM30MGXV2DTHF8A4RE1AG
        task_slug: zero-egress-rooms
        spec_path: docs/features/zero-egress-rooms/spec.md
        branch_name: egress-zero-egress-rooms
        runtime: local
        model: opus
        effort: max
        touches:
          - src/egress.rs
          - src/main.rs
          - src/firecracker.rs
          - src/artifacts.rs
          - src/lib.rs
          - tests/egress_e2e.rs
        status: pending

conflict_notes:
  - kind: dep_signal
    from: egress-control-test-harness
    to: zero-egress-rooms
    reason: "the test harness (sibling task, not in this batch) validates the --egress control this task builds; it is downstream and ships after."

skipped_during_resolution: []
---

# Custody plane driver manifest

Generated by `/work-driver-prep zero-egress-rooms` on 2026-07-23.
Consumed by `/work-driver docs/features/03-custody-plane/driver.md`.

## Batches

### Batch 1 — ready now, 1 stream

- `tsk_01KY6SM30MGXV2DTHF8A4RE1AG` → `docs/features/zero-egress-rooms/spec.md`
  - branch `egress-zero-egress-rooms`
  - **runtime: local** — single-repo host-side networking; the impl is unit-test-driven (`make check`, CI gate), and e2e validation happens on the rooms-host out of band regardless of runtime, so there's no cloud signal that earns the slot. Override to cloud if you'd rather dispatch it and validate e2e afterward.
  - **model: opus / effort: max** — correctness-critical host-side security enforcement. A wrong iptables rule *order* is a silent egress leak (exactly the failure class `isolation.rs` exists to catch); the fail-closed boot/teardown posture and the proof-of-absence receipt need deep, careful reasoning, not mechanical scaffolding.
  - touches: `src/egress.rs` (new), `src/main.rs`, `src/firecracker.rs`, `src/artifacts.rs`, `src/lib.rs`, `tests/egress_e2e.rs` (new)

## Conflict scan

- **File overlap**: none — single stream in this batch.
- **Dep signals**: 1 edge — the sibling `egress-control-test-harness` depends on this task (it validates the `--egress` control). That task is **not** in this batch; it ships after `zero-egress-rooms` merges. When it's prep'd, it becomes Batch 2 with `depends_on: [1]`.

## Reachability

This stream is **local runtime**, so its per-stream worktree forks from `origin/main` — the spec must be on `main` before the driver fans out. Land `docs/features/zero-egress-rooms/spec.md` via a small docs PR (`docs(custody-plane): spec for zero-egress rooms`); never push `main` directly. `/work-driver` fast-forwards the worktree in its pre-flight after the docs PR merges.
