---
driver_version: 1
generated_at: 2026-06-15T00:00:00Z
generated_by: work-driver-prep
source:
  project: rooms
  phase: 01-productionization
repo: rooms
repo_url: https://github.com/itsHabib/rooms
branch_prefix: prod-
default_runtime: cloud

batches:
  - id: 1
    label: ready now (parallel-safe, disjoint files)
    depends_on: []
    status: pending
    streams:
      - task_id: tsk_01KSDN6VY01DKSPX9NPN3ZQVHH
        task_slug: supply-chain-checksums
        spec_path: docs/features/supply-chain-checksums/spec.md
        runtime: cloud
        touches:
          [
            scripts/setup-rooms-host.sh,
            scripts/build-rootfs-alpine.sh,
            scripts/rootfs/install-cursor.sh,
            scripts/checksums.txt,
            src/doctor.rs,
          ]
        status: pending
      - task_id: tsk_01KSDN8TWMW75N47EJ31XGFF6S
        task_slug: harden-tap-rules
        spec_path: docs/features/harden-tap-rules/spec.md
        runtime: cloud
        touches: [scripts/setup-tap.sh, scripts/teardown-tap.sh]
        status: pending

  - id: 2
    label: after batch 1 (rebases onto B1's setup-rooms-host.sh + setup-tap.sh)
    depends_on: [1]
    status: pending
    streams:
      - task_id: tsk_01KSDN5VTSNE820WW3EKF2QCKY
        task_slug: firecracker-under-jailer
        spec_path: docs/features/firecracker-under-jailer/spec.md
        runtime: cloud
        touches:
          [
            src/firecracker.rs,
            src/doctor.rs,
            scripts/setup-rooms-host.sh,
            scripts/setup-tap.sh,
          ]
        status: pending

conflict_notes:
  - kind: file_overlap
    file: scripts/setup-rooms-host.sh
    tasks: [supply-chain-checksums (B1), firecracker-under-jailer (B2)]
    note: |
      supply-chain-checksums adds sha256 verification to the download sites;
      firecracker-under-jailer adds a `firecracker` system user. Different
      regions of the same file — batch-separated by dep ordering so the jailer
      task rebases onto the verified script rather than racing it.
  - kind: file_overlap
    file: scripts/setup-tap.sh
    tasks: [harden-tap-rules (B1), firecracker-under-jailer (B2)]
    note: |
      harden-tap-rules rewrites the NAT/FORWARD rules + ip_forward scoping;
      firecracker-under-jailer changes the TAP owner to the `firecracker` user.
      Batch-separated; jailer rebases onto the hardened rules.
  - kind: file_overlap
    file: src/doctor.rs
    tasks: [supply-chain-checksums (B1, sha-drift check), firecracker-under-jailer (B2, jail checks)]
    note: |
      Both append a new check to the doctor module. Additive + textually
      disjoint, and batch-separated regardless — jailer rebases. Low risk.

runtime_notes:
  merge_gate: rooms-host-e2e
  applies_to: all streams
  decided: 2026-06-15
  policy: |
    Cloud agents write code + unit tests + open PRs + run the 4-bot review.
    The driver drives each PR to review-clean but PAUSES before merge for the
    operator's rooms-host e2e (forced-checksum-mismatch bail; guest egress-ok /
    LAN-blocked; jailer boot as the firecracker user). Cloud VMs have no
    KVM/Firecracker, so the real acceptance can only be verified on rooms-host.
    No admin-merge of unverified security scripts.

skipped_during_resolution: []
---

# Security-hardening driver manifest (rooms 01-productionization)

Generated by `/work-driver-prep` on 2026-06-15. Consumed by `/driver-run` /
`ship driver`. Separate from the original [`driver.md`](driver.md) (the 8-task
v0 productionization manifest, batches 1–3) — this is the security-review
hardening subset (created 2026-05-24), driven as its own batch.

## Why these three

The retroactive security review (2026-05-24) flagged the gap between "works on
my Hyper-V VM" and "safe to run unattended / in cloud." These are the three
zero-cross-dependency hardening tasks; the two rootfs-builder-dependent ones
(`secret-injection-via-vsock`, `readonly-rootfs-with-overlay`) are deferred to
a later batch.

## Batches

### Batch 1 — ready now, 2 streams (parallel-safe, disjoint files)

| task | runtime | spec | touches | merge gate |
|---|---|---|---|---|
| supply-chain-checksums | cloud | `docs/features/supply-chain-checksums/spec.md` | `setup-rooms-host.sh`, rootfs builders, `checksums.txt`, `doctor.rs` | rooms-host e2e |
| harden-tap-rules | cloud | `docs/features/harden-tap-rules/spec.md` | `setup-tap.sh`, `teardown-tap.sh` | rooms-host e2e |

### Batch 2 — after batch 1, 1 stream

| task | runtime | spec | touches | merge gate |
|---|---|---|---|---|
| firecracker-under-jailer | cloud | `docs/features/firecracker-under-jailer/spec.md` | `firecracker.rs`, `doctor.rs`, `setup-rooms-host.sh`, `setup-tap.sh` | rooms-host e2e |

`firecracker-under-jailer` collides with **both** batch-1 streams on a shared
script (`setup-rooms-host.sh` with supply-chain, `setup-tap.sh` with
harden-tap), so it sequences after both land (batch-level `depends_on: [1]`) and
rebases onto their versions.

## Merge gate

Per the 2026-06-15 operator decision (see `runtime_notes` in the frontmatter),
every stream is gated on a **rooms-host e2e**. The driver opens PRs and drives
review to clean, then **stops before merge** for the operator to run the host
e2e on `rooms-host`. Merge (squash, delete branch) only after the operator
confirms the e2e passes.

## Invocation

```
/driver-run docs/features/01-productionization/driver-hardening.md
```
