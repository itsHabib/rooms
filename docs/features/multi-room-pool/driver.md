---
driver_version: 1
generated_at: 2026-07-02T16:20:00Z
generated_by: work-driver-prep
source:
  project: rooms
  phase: multi-room-pool
repo: rooms
repo_url: https://github.com/itsHabib/rooms
branch_prefix: pool-
default_runtime: cloud

batches:
  - id: 1
    label: "P1 slot allocator — pure Rust, no root, CI-gated"
    depends_on: []
    status: done
    streams:
      - task_id: tsk_01KWFVJKNYTEP3AQKAEE6G3F3Z
        task_slug: pool-slot-allocator
        spec_path: docs/features/multi-room-pool/pool-slot-allocator.md
        runtime: cloud
        model: sonnet
        effort: extra
        touches: [src/slot.rs, src/room.rs, src/main.rs, src/error.rs, src/lib.rs]
        status: done
  - id: 2
    label: "P2 tap lifecycle + ROOMS_FWD — gate: single-room host-e2e on rooms-host + adversarial Workflow pass before merge"
    depends_on: [1]
    status: done
    streams:
      - task_id: tsk_01KWFVK8A552FGJCDYP193S5QE
        task_slug: pool-tap-lifecycle-fwd-chain
        spec_path: docs/features/multi-room-pool/pool-tap-lifecycle-fwd-chain.md
        runtime: cloud
        model: opus
        effort: extra
        touches: [src/main.rs, src/firecracker.rs, src/registry.rs, src/slot.rs, src/doctor.rs, src/config.rs, scripts/setup-tap.sh]
        status: done
  - id: 3
    label: "P3 concurrent boot + cap — gate: v0.2 VALIDATION (N=3 host-e2e + ship fan-out smoke) + adversarial pass on isolation asserts"
    depends_on: [2]
    status: done
    streams:
      - task_id: tsk_01KWFVKSDVTQXWTGZK0HNGXVYC
        task_slug: pool-concurrent-boot-cap
        spec_path: docs/features/multi-room-pool/pool-concurrent-boot-cap.md
        runtime: cloud
        model: sonnet
        effort: extra
        touches: [src/main.rs, src/config.rs, src/error.rs, src/slot.rs]
        status: done

conflict_notes:
  - kind: dep_signal
    from: pool-tap-lifecycle-fwd-chain
    to: pool-slot-allocator
    reason: "P2 wires slot::claim/free/reconcile into boot/reap/gc — consumes P1's module (TDD §9 Depends column)"
  - kind: dep_signal
    from: pool-concurrent-boot-cap
    to: pool-tap-lifecycle-fwd-chain
    reason: "P3's cap feeds claim and its e2e exercises P2's taps + chain (TDD §9 Depends column)"
  - kind: file_overlap
    file: src/main.rs
    tasks: [pool-slot-allocator, pool-tap-lifecycle-fwd-chain, pool-concurrent-boot-cap]
    note: "moot — the dep chain already serializes all three batches"
  - kind: file_overlap
    file: src/slot.rs
    tasks: [pool-slot-allocator, pool-tap-lifecycle-fwd-chain, pool-concurrent-boot-cap]
    note: "moot — serialized by dep chain"

skipped_during_resolution: []
---

# Multi-room pool driver manifest

Generated by `/work-driver-prep project:rooms:phase:multi-room-pool` on 2026-07-02.
Consumed by `/work-driver docs/features/multi-room-pool/driver.md`.

Design authority: [multi-room pool TDD](spec.md) (merged as v2 via PR #54 — where a dossier task body and the TDD disagree, the TDD wins; each spec calls out its deltas). Entry gate — the N=1 ship-on-rooms validation — passed 2026-07-02.

## Batches

Strictly serial: P1 → P2 → P3 (TDD §9 dependency order). One stream per batch; parallelism inside this phase is impossible by design, the phase *delivers* parallelism.

| Batch | Task | Spec | Runtime | Model/effort | Gate before next batch |
|---|---|---|---|---|---|
| 1 | `pool-slot-allocator` | [pool-slot-allocator.md](pool-slot-allocator.md) | cloud | sonnet/extra | unit + proptest green in CI (`make check`) |
| 2 | `pool-tap-lifecycle-fwd-chain` | [pool-tap-lifecycle-fwd-chain.md](pool-tap-lifecycle-fwd-chain.md) | cloud | opus/extra | single-room host-e2e on rooms-host; **adversarial Workflow pass** before merge |
| 3 | `pool-concurrent-boot-cap` | [pool-concurrent-boot-cap.md](pool-concurrent-boot-cap.md) | cloud | sonnet/extra | **v0.2 VALIDATION GATE**: N=3 concurrent host-e2e, zero cross-talk, ship fan-out smoke; adversarial pass on isolation asserts |

## Runtime + gate notes

- **Cloud runtime** per this repo's convention (CLAUDE.md cloud defaults): agents implement + run `make check` (unit tests only — no KVM in cloud VMs). Host-e2e is authored by the agents as feature-gated tests but **executed on rooms-host** at each batch gate.
- Batches 2 and 3 are firewall/isolation surfaces — each needs the **adversarial Workflow pass** (escape the /30, reach a sibling guest, leak a slot) in addition to the @codex/@claude/@cursor panel, per the gate-change convention.
- P3's gate includes recording real N=3 memory/CPU on the dossier task (informs the default cap).
